<h1 style="color: black; font-weight: bold; ">Bienvenue {{ user.prenom }} {{ user.nom }} !</h1>

<p>Vous vous êtes inscrit avec le mail : <code>{{ user.email }}</code></p>
<p>Votre mot de passe est : <code> {{ plain_password }} </code></p>

<p>
    <a href="{{ host }}/activate/user/{{ user.id }}" class="btn btn-dark btn-lg" style="
    display: inline-block;
    font-weight: 400;
        color: #fff;
        background-color: #343a40;
        border-color: #343a40;
    text-align: center;
        vertical-align: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        background-color: transparent;
        border: 1px solid transparent;
        padding: .375rem .75rem;
        font-size: 1rem;
        line-height: 1.5;
        border-radius: .25rem;
        transition: color .15s ease-in-out,background-color .15s ease-in-out,border-color .15s ease-in-out,box-shadow .15s ease-in-out
        padding: .5rem 1rem;
        font-size: 1.25rem;
        line-height: 1.5;
        border-radius: .3rem;
">Cliquez ici pour activez votre compte</a>
</p>