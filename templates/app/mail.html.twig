<style>
    .btn-lg {
        padding: .5rem 1rem;
        font-size: 1.25rem;
        line-height: 1.5;
        border-radius: .3rem;
    }
    .btn-dark {
        color: #fff;
        background-color: #343a40;
        border-color: #343a40;
    }
    .btn {
        display: inline-block;
        font-weight: 400;
        color: #212529;
        text-align: center;
        vertical-align: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        background-color: transparent;
        border: 1px solid transparent;
        padding: .375rem .75rem;
        font-size: 1rem;
        line-height: 1.5;
        border-radius: .25rem;
        transition: color .15s ease-in-out,background-color .15s ease-in-out,border-color .15s ease-in-out,box-shadow .15s ease-in-out;
    }
</style>
<h1 style="color: black; font-weight: bold; ">Bienvenue {{ user.firstname }} {{ user.lastname }} !</h1>

<p>Vous vous êtes inscrit avec le mail : <code>{{ user.mail }}</code></p>

<p>
    <a href="http://localhost:8000/activate/{{ user.id }}" class="btn btn-dark btn-lg">Cliquez ici pour activez votre compte</a>
    (Ce lien est valide jusqu'au {{ expiration_date|date('F jS') }})
</p>