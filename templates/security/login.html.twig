<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Log in!{% endblock %}</title>
        <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet"/>
        <link href="{{ asset('css/font-awesome.min.css') }}" rel="stylesheet"/>
        <link href="{{ asset('css/app.css') }}" rel="stylesheet"/>
        {% block stylesheets %} {% endblock %}
    </head>

    <body>
        {% block body %}
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg box-login">
                        <div class="row justify-content-center">
                            <div class="col-md-8 align-self-center">
                                <form method="post">
                                {% if app.user %}
                                    <div class="mb-3">
                                        You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                                    </div>
                                {% endif %}

                                <h1 class="h1 mb-3 text-center font-weight-normal">Connectez vous sur <strong>SymfAgents</strong></h1>
                                <label for="inputEmail">Email</label>
                                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
                                <label for="inputPassword">Password</label>
                                <input type="password" name="password" id="inputPassword" class="form-control" required>

                                <input type="hidden" name="_csrf_token"
                                    value="{{ csrf_token('authenticate') }}"
                                >

                                {#
                                    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                                    See https://symfony.com/doc/current/security/remember_me.html
                                
                                    <div class="checkbox mb-3">
                                        <label>
                                            <input type="checkbox" name="_remember_me"> Remember me
                                        </label>
                                    </div>
                                #}
                                {% if error %}
                                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                {% endif %}
                                <div class="text-center">
                                    <button class="btn btn-lg btn-dark mt-4" type="submit">
                                        Connectez vous
                                    </button>
                                </div>
                                
                                </form>
                            </div>
                        </div>
                        
                    </div>
                    <div class="col-lg bg-dark box-login">
                        <div class="row justify-content-center">
                            <div class="col-md-8">
                                <h1 class="h1 mb-3 text-center text-light">Bonjour  l'ami</h1>
                                <h4 class="text-light text-center">Connectes toi dans notre plateforme et commence le voyage avec nous</h4>
                                <div class="text-center mt-4">
                                    <a href="/register" class="btn btn-outline-light btn-lg">Enregistrez vous</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
        {% endblock %}
        <script src="{{ asset('js/jquery-3.3.1.slim.min.js') }}"></script>
        <script src="{{ asset('js/popper.min.js') }}"></script>
        <script src="{{ asset('js/bootstrap.min.js') }}"></script>
        <!-- bootbox code -->
        <script src="{{ asset('js/bootbox.min.js') }}"></script>
        <script src="{{ asset('js/bootbox.locales.min.js') }}"></script>
        <script src="{{ asset('js/app.js') }}"></script>
    </body>
</html>
